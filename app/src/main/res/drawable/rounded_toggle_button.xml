<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 평상시 -->
    <item
        android:state_checked="false">
        <bitmap android:src="@drawable/round_expand_more_black_36dp"
            android:tint="@color/DarkModeBlack"/>

    </item>

    <!-- 클릭했을 때 -->
    <item
        android:state_checked="true">
        <bitmap android:src="@drawable/round_expand_less_black_36dp"
            android:tint="@color/DarkModeBlack"/>

    </item>

<!--    &lt;!&ndash; check 때 &ndash;&gt;
    <item
        android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/colorAccent" />
            <corners android:radius="15dp"/>
            <size
                android:width="36dp"
                android:height="50dp"/>
            <stroke
                android:width="1dp"
                android:color="@color/colorAccent" />
        </shape>
    </item>

    <item
        android:state_checked="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/colorAccent" />
            <corners android:radius="15dp"/>
            <size
                android:width="36dp"
                android:height="50dp"/>
            <stroke
                android:width="1dp"
                android:color="@color/colorAccent" />
        </shape>
    </item>-->


</selector>
